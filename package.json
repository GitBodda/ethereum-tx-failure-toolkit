{
  "name": "ethereum-tx-failure-toolkit",
  "version": "1.0.0",
  "description": "Minimal CLI to analyze Ethereum transaction failures.",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "txft": "bin/txft"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsx src/index.ts",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "tsx": "^4.0.0",
    "@types/node": "^20.0.0",
    "vitest": "^1.0.0"
  }
}
